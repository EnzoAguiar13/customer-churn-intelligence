# Non-secret local defaults for the Phase 5A bootstrap.
CCI_ENVIRONMENT=development
CCI_LOG_LEVEL=INFO

# Future integrations must use local environment variables or CI secrets.
# Never commit database credentials, MLflow credentials, API keys or tokens.

